ignore:
  # R-E98AA: all 21 flagged inputs are non-email fields — search boxes,
  # usernames, names, security answers, admin credential fields (intentional
  # plaintext so admins can see the password they're setting), page-title
  # editors, test-run labels, and the anti-bot honeypot. Actual email fields
  # already use type="email" (admin.users.tsx:354, :439, :869). Fully traced.
  - R-E98AA
