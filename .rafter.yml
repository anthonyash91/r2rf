suppress:
  # R-E98AA: remaining type="text" inputs are not email fields.
  # Confirmed by tracing all 18 remaining locations:
  #   - admin.users.tsx: 4 admin credential fields (intentional plaintext
  #     so admins can read the password they're setting for another account)
  #     + 1 username field
  #   - signup.tsx: username, security question answers, first/last name,
  #     username-or-email sign-in field (accepts both formats), honeypot
  #   - admin.privacy/terms.tsx: 4 page-title editors
  #   - dashboard.tsx: QA test-run label input
  #   - SecurityQuestionsForm.tsx: security answer text field
  # The 3 search inputs (BulkActionBar, admin.category, admin.index) have
  # been fixed at source to type="search". Actual email fields already
  # use type="email" (admin.users.tsx:354, :439, :869).
  - id: R-E98AA
